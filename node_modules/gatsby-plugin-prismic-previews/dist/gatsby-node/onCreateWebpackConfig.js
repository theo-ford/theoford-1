import { getPublicModelsFileName } from "../lib/getPublicModelsFileName.js";
const onCreateWebpackConfig = async (args, _options) => {
  const publicModelsFileName = await getPublicModelsFileName(args.cache);
  args.actions.setWebpackConfig({
    plugins: [
      args.plugins.define({
        __PUBLIC_MODELS_PATH__: JSON.stringify(`/static/${publicModelsFileName}`)
      })
    ]
  });
};
export {
  onCreateWebpackConfig
};
//# sourceMappingURL=onCreateWebpackConfig.js.map
