import { usePrismicPreviewStore } from "../usePrismicPreviewStore.js";
const getRepositoryConfig = (repositoryName) => {
  const state = usePrismicPreviewStore.getState();
  return state.repositoryConfigs.find((config) => config.repositoryName === repositoryName);
};
export {
  getRepositoryConfig
};
//# sourceMappingURL=getRepositoryConfig.js.map
