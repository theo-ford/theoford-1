import { createContentDigest } from "gatsby-core-utils";
import { PUBLIC_MODELS_FILE_NAME_SEED_CACHE_KEY } from "../constants.js";
const getPublicModelsFileName = async (cache) => {
  let seed = await cache.get(PUBLIC_MODELS_FILE_NAME_SEED_CACHE_KEY);
  if (!seed) {
    seed = Date.now().toString();
    await cache.set(PUBLIC_MODELS_FILE_NAME_SEED_CACHE_KEY, seed);
  }
  return `${createContentDigest(seed)}.json`;
};
export {
  getPublicModelsFileName
};
//# sourceMappingURL=getPublicModelsFileName.js.map
