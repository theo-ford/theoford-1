import { Client, PrismicDocument } from "@prismicio/client";
type FetchNewDocumentsArgs = {
    client: Client;
    abortController: AbortController;
};
export declare const fetchNewDocuments: ({ client, abortController, }: FetchNewDocumentsArgs) => Promise<PrismicDocument[]>;
export {};
