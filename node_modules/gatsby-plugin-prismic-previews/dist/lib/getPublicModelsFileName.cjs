"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const gatsbyCoreUtils = require("gatsby-core-utils");
const constants = require("../constants.cjs");
const getPublicModelsFileName = async (cache) => {
  let seed = await cache.get(constants.PUBLIC_MODELS_FILE_NAME_SEED_CACHE_KEY);
  if (!seed) {
    seed = Date.now().toString();
    await cache.set(constants.PUBLIC_MODELS_FILE_NAME_SEED_CACHE_KEY, seed);
  }
  return `${gatsbyCoreUtils.createContentDigest(seed)}.json`;
};
exports.getPublicModelsFileName = getPublicModelsFileName;
//# sourceMappingURL=getPublicModelsFileName.cjs.map
