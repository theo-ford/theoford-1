import * as React from "react";
import type { PagePropsLike } from "./types";
export type WithPrismicPreviewProps<TProps = Record<string, unknown>> = {
    originalData: TProps;
    isPrismicPreview: boolean;
};
export declare const withPrismicPreview: <TProps extends PagePropsLike>(WrappedComponent: React.ComponentType<TProps>) => React.ComponentType<TProps>;
