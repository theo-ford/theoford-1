import { jsx } from "react/jsx-runtime";
import { getToolbarSrc } from "@prismicio/client";
const onRenderBody = (args, options) => {
  const toolbarURL = new URL(getToolbarSrc(options.repositoryName));
  args.setHeadComponents([
    /* @__PURE__ */ jsx("link", { rel: "preconnect", href: toolbarURL.origin }, "preconnect-prismic-toolbar"),
    /* @__PURE__ */ jsx("link", { rel: "dns-prefetch", href: toolbarURL.origin }, "dns-prefetch-prismic-toolbar")
  ]);
  args.setPostBodyComponents([
    /* @__PURE__ */ jsx("script", { src: toolbarURL.href, defer: true }, `prismic-toolbar-${options.repositoryName}`)
  ]);
};
export {
  onRenderBody
};
//# sourceMappingURL=onRenderBody.js.map
